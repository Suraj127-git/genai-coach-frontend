import { cssInterop } from 'nativewind'
import { Box, Text, Button, ButtonText, Input, InputField } from '@gluestack-ui/themed'

cssInterop(Box, { className: 'style' })
cssInterop(Text, { className: 'style' })
cssInterop(Button, { className: 'style' })
cssInterop(ButtonText, { className: 'style' })
cssInterop(Input, { className: 'style' })
cssInterop(InputField, { className: 'style' })